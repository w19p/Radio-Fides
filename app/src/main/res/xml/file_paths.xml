<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Archivos .txt privados -->
    <files-path name="grabaciones_metadata" path="Grabaciones radio fides/" />

    <!-- Archivos .mp3 en Music público — ruta completa desde raíz -->
    <external-path name="grabaciones_audio" path="." />
</paths>
